/**
 * Test Setup
 *
 * Configures testing environment.
 */

import '@testing-library/jest-dom';

// Tests use the real .NET API on localhost:5180
// If tests need mocking, consider using vitest's mocking capabilities
